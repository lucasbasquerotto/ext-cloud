- name: "{{ params.title }} - task params & credentials (cloud)"
  set_fact:
    task_params: "{{ params.task.params }}"
    task_credentials: "{{ params.task.credentials }}"
  tags: ["no_print"]

- name: "{{ params.title }} - print params (cloud)"
  debug:
    var: task_params

- name: "{{ params.title }} - print credentials (cloud)"
  debug:
    var: task_credentials

- name: "{{ inner_service_title }} - print (test) - 03 - params"
  debug:
    var: inner_service_params
  tags: ["no_print_skipped"]

- name: "{{ inner_service_title }} - print (test) - 03 - credentials"
  debug:
    var: inner_service_credentials
  tags: ["no_print_skipped"]

- name: "{{ inner_service_title }} - print (test) - 03 - contents"
  debug:
    var: inner_service_contents
  tags: ["no_print_skipped"]

- name: "{{ inner_service_title }} - print (test) - 03 - state"
  debug:
    var: inner_service_state
  tags: ["no_print_skipped"]

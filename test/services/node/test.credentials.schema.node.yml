root: "credentials"
schemas:
  credentials:
    type: "dict"
    props:
      node:
        schema: "test_credentials"
        non_empty: true
  test_credentials:
    type: "dict"
    props:
      test_token:
        type: "str"
        non_empty: true

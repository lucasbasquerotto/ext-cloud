root: "docker_params"
schemas:
  docker_params:
    type: "dict"
    props:
      start_service:
        type: "bool"
      compose:
        schema: "compose"
  compose:
    type: "dict"
    props:
      src:
        type: "str"
      version:
        type: "str"
      dest:
        type: "str"
      mode:
        type: "int"
